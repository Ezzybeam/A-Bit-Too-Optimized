# Shared project properties
org.gradle.jvmargs=-Xmx2G
org.gradle.parallel=true
# JDK 25 is required to build the modern Minecraft 26.x branches. It is installed
# in a user directory (no sudo) and pointed to here so Gradle's Java toolchain can
# find it. Update this path if the JDK location changes.
org.gradle.java.installations.paths=/Users/ezzybeam/.jdks/jdk-25.0.3+9/Contents/Home

mod_id=abto
# Bump the minor each completed milestone: M1=0.1.0, M2=0.2.0, M3=0.3.0, ...
# (reaches 1.0.0 at the first real public release).
mod_version=0.7.5
mod_name=A Bit Too Optimized
maven_group=com.abto
